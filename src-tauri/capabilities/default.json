{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Claude widget window.",
  "windows": ["claude-widget"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
